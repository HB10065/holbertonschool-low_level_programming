#include "main.h"
/**
 * _abs - calcuates the absolute value
 * @n: given number tu calulate
 * Return: n fi n is positive or zero , -n if n is negative
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
